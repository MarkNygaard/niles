# Documentation source

[mdBook](https://rust-lang.github.io/mdBook/) source for the public Niles documentation site. Populated in **Phase 11**.

The capability reference files used by the runtime ([`niles-capabilities`](../../crates/niles-capabilities/)) and the user-facing per-feature docs are generated from the same source — they cannot drift.
