# ESP-IDF generated / downloaded artifacts — not source.
/build/
/managed_components/
/sdkconfig
/sdkconfig.old
/dependencies.lock

# Editor / IDE config with machine-specific absolute paths.
/.vscode/
/.clangd

# Local secrets (WiFi creds, host) — copy from secrets.example.h.
/main/secrets.h
