# Native Rust satellite firmware

Future option: a satellite firmware written in Rust using `esp-hal` / `embassy`. ESPHome (see [`../esphome/`](../esphome/)) is the recommended starting point for everyone in v1; this directory exists to mark the eventual landing spot for a Rust-native alternative.

Not in scope for v1.
