# Docker Compose deployment

Reference `docker-compose.yml` and `.env.example` for the default single-host deployment path. Populated in **Phase 11** per [ARCHITECTURE.md](../../ARCHITECTURE.md#docker-compose-default-path).

Services included will be: Mosquitto, Zigbee2MQTT, Piper TTS, and Niles itself.
