# Niles Dockerfile

Multi-stage Dockerfile used by both the Compose path ([`../compose/`](../compose/)) and the Kubernetes path ([`../kubernetes/`](../kubernetes/)) to build the `niles` image.

Populated in **Phase 11**.
