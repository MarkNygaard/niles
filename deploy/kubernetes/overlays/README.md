# Kubernetes overlays

Per-home Kustomize overlays that patch the base with user-specific values (coordinates, presence sources, API tokens, etc.).

Will include example overlays for `example-home/` and `dev/`. Populated in **Phase 11**.
